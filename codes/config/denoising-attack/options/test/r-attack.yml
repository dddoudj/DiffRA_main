name: sigma50-0.0001-0.5
suffix: ~  # add suffix to saved images
model: denoising
gpu_ids: [0]

sde:
  max_sigma: 70
  T: 1000

degradation:
  sigma: 50
  noise_type: G # Gaussian noise: G

datasets:
  test1:
    name: McMaster
    mode: GT
    dataroot_GT: /home//refusion/dataset/testGT

#### network structures
network_G:
  which_model_G: ConditionalNAFNet
  setting:
    width: 64
    enc_blk_nums: [1, 1, 1, 28]
    middle_blk_num: 1
    dec_blk_nums: [1, 1, 1, 1]

#### path
path:
  pretrain_model_G: /home//image-restoration-sde-main/codes/pretrained/pretrained-denoising/attack-denoising.pth
